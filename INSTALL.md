# 鸡语声波 · Windows 桌面版安装与使用指南

鸡语声波桌面版将原有的网页应用封装为 Windows 原生程序：根据当地温度、湿度、气候与阳光照射率，通过 Web Audio API 实时合成相应特征的声波，供养殖户与爱好者观察、试验。

> 科学边界声明：本项目仅根据环境参数生成声波，供观察与试验。「特定声波能提高产蛋率/改变鸡叫频率」目前缺乏充分、严谨的同行评议证据，请勿将本工具输出作为养殖决策依据，勿让鸡群长时间暴露高分贝声环境。

---

## 1. 系统要求

| 项目 | 要求 |
| --- | --- |
| 操作系统 | Windows 10 / Windows 11（64 位） |
| 架构 | x64 |
| 内存 | 建议 4 GB 及以上 |
| 网络 | 可选；「自动获取当地天气」需联网访问 Open-Meteo（免费、免密钥），断网时可手动滑杆设置参数 |

## 2. 安装步骤

1. 双击 `ChickenSoundWaves-Setup-1.1.0.exe`；
2. 安装向导默认安装到当前用户目录（无需管理员权限），可按需修改安装路径；
3. 向导会自动创建桌面快捷方式与开始菜单项；
4. 点击桌面「鸡语声波」图标即可运行。

### 静默安装（可选）

```bat
ChickenSoundWaves-Setup-1.1.0.exe /S /D=D:\ChickenSoundWaves
```

注意：`/D=` 必须放在命令最后且不带引号。

## 3. 使用说明

- **自动获取当地天气**：点击「自动获取当地天气」按钮，Windows 会弹出定位授权提示，允许后程序将按当地经纬度请求 Open-Meteo 实时天气（温度/湿度/天气代码/日照率），并联动更新声波参数；
- **手动设置**：首次打开或定位失败时，可直接拖动温度/湿度/阳光照射率滑杆、选择天气状况；
- **播放/停止**：点击「播放声波」实时合成声波（带响亮的“咯-咯”双音听感），「停止」即停止；音量可调；
- 页面底部实时绘制声波波形，并展示环境舒适度指数（0–100）与四个环境因子的分项得分。

## 4. 卸载

- 开始菜单 →「鸡语声波」→「卸载鸡语声波」，或
- 打开安装目录，运行 `Uninstall 鸡语声波.exe`，或
- 控制面板 → 程序和功能 → 卸载。

卸载会移除程序文件与快捷方式；不删除浏览器缓存与用户数据（如未来版本产生的本地设置）。

## 5. 常见问题

| 问题 | 说明与处理 |
| --- | --- |
| Windows SmartScreen 提示「未知发布者」 | 本应用未购买代码签名证书，属于正常提示。点击「更多信息」→「仍要运行」；若从网络下载，亦可在文件属性中「解除锁定」。 |
| 杀毒软件误报 | 未签名 Electron 应用偶尔被启发式引擎误报。可先对安装包右键扫描确认后添加信任；建议从官方渠道获取安装包。 |
| 定位失败/天气获取失败 | 程序自动回落到手动滑杆模式。检查系统定位开关（设置 → 隐私 → 位置）与网络连通性。 |
| 无网络环境 | 可完全离线使用手动参数模式；声波合成不依赖网络。 |
| 安装后无法启动 | 确认系统为 Windows 10 及以上；删除安装目录后重新安装。 |
| 音量/声波无输出 | 检查系统音量与扬声器设备；部分虚拟音频设备可能不兼容 Web Audio。 |

## 6. 从源码重新构建

需要 Node.js 20+ 与 npm：

```bash
npm install
npm run dist        # 产出 release/ChickenSoundWaves-Setup-<version>.exe
```

本地开发调试：`npm start`；打包前自检：`npm run smoke`。

## 7. 隐私与数据

- 定位仅用于请求本地天气，不存储、不上传、不发送给任何第三方；
- 天气数据来自 Open-Meteo（https://open-meteo.com），请求参数仅含经纬度；
- 应用不包含任何遥测、统计或广告组件。

## 8. 目录结构（桌面工程）

```
chicken-sound-desktop/
├── main.js            # Electron 主进程（窗口/权限/外链处理）
├── package.json       # 依赖与 electron-builder 打包配置
├── index.html         # 应用页面（与网页版一致）
├── css/  js/          # 样式与算法/天气/声波合成脚本
├── build/             # 应用图标（icon.ico / icon.png）
├── INSTALL.md         # 本文档
└── LICENSE            # MIT
```